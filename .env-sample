# Cookie for an authenticated target (chaise pages are usually not anonymous).
# This is the value of the webauthn cookie for the target host.
LOAD_TEST_COOKIE="webauthn=..."

# Optional default chaise base url of the target.
LOAD_TEST_BASE_URL="https://HOST/chaise/"
